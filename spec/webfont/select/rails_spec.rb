require 'spec_helper'

describe Webfont::Select::Rails do
  it 'has a version number' do
    expect(Webfont::Select::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
